{{/*
IMPORTANT: This is a TEMPLATE only.
You MUST create the actual secret manually before deployment.
This file shows the structure but should NOT contain real secrets.
*/}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "laravel-vue-app.fullname" . }}-secrets
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "laravel-vue-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  # ⚠️ DO NOT COMMIT REAL SECRETS TO GIT!
  # Create this secret manually using kubectl:
  # kubectl create secret generic laravel-vue-app-secrets \
  #   --namespace=production \
  #   --from-literal=APP_KEY="your-app-key" \
  #   --from-literal=DB_HOST="your-db-host" \
  #   --from-literal=DB_PORT="3306" \
  #   --from-literal=DB_USERNAME="root" \
  #   --from-literal=DB_PASSWORD="your-password" \
  #   --from-literal=JWT_SECRET="your-jwt-secret"
  
  # Placeholder structure only:
  APP_KEY: "CHANGE-ME"
  DB_HOST: "CHANGE-ME"
  DB_PORT: "CHANGE-ME"
  DB_USERNAME: "CHANGE-ME"
  DB_PASSWORD: "CHANGE-ME"
  JWT_SECRET: "CHANGE-ME"
